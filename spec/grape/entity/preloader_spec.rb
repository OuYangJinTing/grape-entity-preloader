# frozen_string_literal: true

RSpec.describe Grape::Entity::Preloader do
  it 'has a version number' do
    expect(Grape::Entity::Preloader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
